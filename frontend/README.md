# Interline frontend

Next.js 15 App Router UI for the bilateral credit line. See the root `README.md` for install, Anvil, deploy, and the eight demo beats.

```bash
cp ../.env.example .env.local
# paste NEXT_PUBLIC_* addresses after forge script deploy
npm i
npm run dev
```
