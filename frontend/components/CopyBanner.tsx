export function CopyBanner() {
  return (
    <p className="border-b border-zinc-800 bg-zinc-900/60 px-4 py-2 text-sm text-zinc-300">
      The new limit is hashed until both sides sign. Then it becomes public.
    </p>
  );
}
